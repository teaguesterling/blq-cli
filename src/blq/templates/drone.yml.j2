# Generated by blq v{{ blq_version }}
# Regenerate with: blq hooks install drone {{ commands | map(attribute='name') | join(' ') }} --force
#
# Include in .drone.yml or use directly as .drone.yml

kind: pipeline
type: docker
name: blq-checks

steps:
{% for cmd in commands %}
  - name: {{ cmd.name }}
    image: alpine
    commands:
      - .lq/hooks/{{ cmd.name }}.sh --via=standalone --metadata=footer
{% endfor %}
