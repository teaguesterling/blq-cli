# Generated by blq v{{ blq_version }}
# Regenerate with: blq hooks install gitlab {{ commands | map(attribute='name') | join(' ') }} --force
#
# Include in .gitlab-ci.yml:
#   include:
#     - local: '.gitlab-ci.blq.yml'

{% for cmd in commands %}
blq-{{ cmd.name }}:
  script:
    - .lq/hooks/{{ cmd.name }}.sh --via=standalone --metadata=footer
{% if cmd.description %}
  # {{ cmd.description }}
{% endif %}

{% endfor %}
